{
  "name": "evoclaw",
  "version": "0.1.0",
  "private": true,
  "description": "EvoClaw - Self-evolving AI Companion Desktop App",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "dev:core": "turbo dev --filter=@evoclaw/core",
    "build:desktop": "turbo build --filter=@evoclaw/desktop"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.2"
  },
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
