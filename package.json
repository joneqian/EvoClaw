{
  "name": "evoclaw",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "dev:core": "turbo run dev --filter=@evoclaw/core",
    "build:desktop": "turbo run build --filter=@evoclaw/desktop"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "oxlint": "^0.17.0",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
